[pytest]
DJANGO_SETTINGS_MODULE = flvs_project.settings.test
python_files = tests.py test_*.py *_tests.py
# --no-migrations: Schema direkt aus Models erzeugen, weil eine
# Postgres-spezifische RunSQL-Migration in procurement auf SQLite scheitert.
addopts = --tb=short --no-migrations
